#SET - disordered collection with unique value
#SET collection may contain immutable data type: digit, bool, string, couple. List, dictionary,
#other collection doesn't use
s_1 = set() #Empty set
s = {1,2,5,'a'} #Add value to collection
s.add('b') #Add value to collection, if you try to add the same value, duplicate will be delete
s_2 = set([1,3,'5']) #In func set we can pass iter value, as example: List of var, string var ...
s_2 = set('abra')
print(s_2)

#iterationg over the collection
for c in s_2:
    print(c)
#check element to contant in collection
print(True if 'c' in s_2 else False)

s.update(['b', 3, 2]) #In fuct update we can pass iter value. This func can add few or more value's
print(s)
s.discard(3) #discard method remove value, if value in collection doesn't exist, exception will not work
print(s)
s.remove(2) #remove method, remove value from collection, if value doesn't exist raise exeption
print(s)
s.pop()
print(s) # pop method remove random value from collection
